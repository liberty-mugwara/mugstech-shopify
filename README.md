# Mugstech shopify package

Mugstech shopify package uses the Shopify GraphQL API.

The following environment variables are required:

- `SHOPIFY_PASSWORD`
- `SHOPIFY_USERNAME`
- `SHOPIFY_API_VERSION`
